{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "definitions": {
        "ChineseOnly": {
            "description": "manhua.uk「只看中文漫画」开关。开启后该源的发现页 / 搜索页\r\n列表请求统一带 language=zh（由 saga 注入到 filter），仅返回中文漫画。\r\n持久化在 setting.chineseOnly，与具体源解耦的存储位（当前仅 manhua.uk 消费）。",
            "enum": [
                0,
                1
            ],
            "type": "number"
        },
        "IconLabel": {
            "enum": [
                0,
                1
            ],
            "type": "number"
        },
        "LayoutMode": {
            "description": "enum layoutmode of Reader component",
            "enum": [
                "horizontal",
                "vertical",
                "multiple"
            ],
            "type": "string"
        },
        "MultipleSeat": {
            "enum": [
                0,
                1
            ],
            "type": "number"
        },
        "PageKeys": {
            "enum": [
                0,
                1
            ],
            "type": "number"
        },
        "ReaderDirection": {
            "enum": [
                "left",
                "right"
            ],
            "type": "string"
        },
        "Sequence": {
            "enum": [
                "Asc",
                "Desc"
            ],
            "type": "string"
        },
        "ThemeMode": {
            "description": "应用外观偏好；System 由 Android 当前 uiMode 解析为亮色或深色。",
            "enum": [
                "light",
                "dark",
                "system"
            ],
            "type": "string"
        },
        "Timer": {
            "enum": [
                0,
                1
            ],
            "type": "number"
        }
    },
    "properties": {
        "androidDownloadPath": {
            "type": "string"
        },
        "chineseOnly": {
            "$ref": "#/definitions/ChineseOnly",
            "description": "manhua.uk「只看中文漫画」：开启后该源发现页/搜索页统一带 language=zh"
        },
        "direction": {
            "$ref": "#/definitions/ReaderDirection",
            "description": "漫画阅读方向"
        },
        "iconLabel": {
            "$ref": "#/definitions/IconLabel",
            "description": "图标按钮下方是否显示说明文字"
        },
        "mode": {
            "$ref": "#/definitions/LayoutMode",
            "description": "布局模式"
        },
        "pageKeys": {
            "$ref": "#/definitions/PageKeys",
            "description": "是否监听实体翻页键"
        },
        "seat": {
            "$ref": "#/definitions/MultipleSeat",
            "description": "双页模式的图片位置"
        },
        "sequence": {
            "$ref": "#/definitions/Sequence",
            "description": "章节排列顺序"
        },
        "themeMode": {
            "$ref": "#/definitions/ThemeMode",
            "description": "应用外观：亮色、深色或跟随系统"
        },
        "timer": {
            "$ref": "#/definitions/Timer",
            "description": "定时翻页"
        },
        "timerGap": {
            "type": "number"
        }
    },
    "required": [
        "androidDownloadPath",
        "chineseOnly",
        "direction",
        "iconLabel",
        "mode",
        "pageKeys",
        "seat",
        "sequence",
        "themeMode",
        "timer",
        "timerGap"
    ],
    "type": "object"
}

